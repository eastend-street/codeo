import React from "react";
import styled from "styled-components";

const Container = styled.div`

`

const Nav: React.FC = () => {
  return (
    <Container>
      <div>React</div>
    </Container>
  );
};

export default Nav;
